import { FieldAriaAttributes } from './field-aria-attributes';

describe('FieldAriaAttributes', () => {
  it('should create an instance', () => {
    const directive = new FieldAriaAttributes();
    expect(directive).toBeTruthy();
  });
});
